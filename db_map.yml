host: "localhost"
port: 5432
user: postgres
password: postgres
database: orders

tables:
  - name: order_states
    columns:
      - name: order_id
        type: text
        source: [ orderId ]
        index: true
      - name: state
        type: text
        source: [ states, name ]
        index: true